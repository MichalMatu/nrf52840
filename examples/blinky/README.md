# Blinky Example

The main application already starts as a minimal blinky firmware.

For throwaway experiments, copy `src/main.cpp` into a new directory under
`examples/` and add a dedicated PlatformIO environment before promoting the
code back into `src/`.

